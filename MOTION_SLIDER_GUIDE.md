# Motion Slider - Quick Guide

## 📁 Файли компонента

- **CSS:** `/static/css/motion-slider.css`
- **JS:** `/static/js/motion-slider.js`
- **HTML:** Інтегрований в `menssuite.html` та `private.html`

## 🖱️ Як працює Custom Cursor

### Поведінка:
- Над слайдером **стандартний курсор зникає**
- З'являється **custom cursor** (білий кружок зі стрілкою)
- **Ліва половина екрану** → стрілка **ліворуч** ← (перехід назад)
- **Права половина екрану** → стрілка **праворуч** → (перехід вперед)
- **Клік** миші = миттєвий перехід + прогрес починається з 0
- **Прогрес-кільце** навколо стрілки заповнюється за 5.5 сек → автоперехід + прогрес рестартує
- **Зміна напрямку** (ліво↔право) = прогрес скидається на 0 і починається знову
- **Після перемикання** слайду = прогрес автоматично починається з 0

### Автопрогрес:
- **Без миші:** Слайдер сам крутиться кожні **11 сек** (авто-режим)
- **З мишею:** Прогрес-кільце контрольоване курсором
  - Запускається прогрес-кільце (контрольоване курсором)
  - Перемикання після завершення прогресу (**5.5 сек**) або кліку
  - Після перемикання прогрес автоматично рестартує
  - **Безкінечний цикл** поки миша над слайдером
- **Миша виходить:** Повертається автопрогрес (11 сек без кільця)

### На мобілках:
- Custom cursor не показується (немає миші)
- Кнопки зліва/справа + swipe жести
- Автопрогрес працює постійно

## 🎬 Як замінити зображення на відео

### Крок 1: Завантажте відео
Помістіть відео в `/static/videos/` (або `/media/` якщо через Django)

### Крок 2: Змініть HTML слайду

**Було (зображення):**
```html
<article class="motion-slide">
    <img src="{% static 'images/treatment_room.png' %}" alt="AI Massage Bed">
    <div class="motion-slide-content">
        <h2>AI Massage<br>Therapy</h2>
        <p>Advanced technology for ultimate relaxation</p>
    </div>
</article>
```

**Стало (відео):**
```html
<article class="motion-slide">
    <video autoplay loop muted playsinline>
        <source src="{% static 'videos/massage-therapy.mp4' %}" type="video/mp4">
        Your browser doesn't support video.
    </video>
    <div class="motion-slide-content">
        <h2>AI Massage<br>Therapy</h2>
        <p>Advanced technology for ultimate relaxation</p>
    </div>
</article>
```

### Важливо:
- `autoplay` - автоматичне відтворення
- `loop` - зациклення відео
- `muted` - без звуку (потрібно для autoplay)
- `playsinline` - відтворення inline на iOS

## ✏️ Як змінити текст слайду

### Заголовок:
```html
<h2>Ваш заголовок<br>Другий рядок</h2>
```
- `<br>` для перенесення рядка
- Великі літери для консистентності з дизайном

### Підзаголовок:
```html
<p>Ваш опис слайду</p>
```

## 🎨 Як змінити позицію тексту

За замовчуванням текст зліва. Щоб змінити, додайте CSS в `motion-slider.css`:

```css
/* Текст праворуч */
.motion-slide-content.right {
    left: auto;
    right: 8%;
}

/* Текст по центру */
.motion-slide-content.center {
    left: 50%;
    right: auto;
    transform: translate(-50%, -50%);
    text-align: center;
}
```

І додайте клас в HTML:
```html
<div class="motion-slide-content right">
```

## ➕ Як додати новий слайд

### 1. Додайте HTML слайду:
```html
<!-- Після останнього слайду -->
<article class="motion-slide">
    <img src="{% static 'images/new-amenity.png' %}" alt="New Amenity">
    <div class="motion-slide-content">
        <h2>New<br>Feature</h2>
        <p>Description of new feature</p>
    </div>
</article>
```

### 2. Додайте dots navigation:
```html
<ul class="motion-dots">
    <li class="motion-dot active"></li>
    <li class="motion-dot"></li>
    <li class="motion-dot"></li>
    <li class="motion-dot"></li>
    <li class="motion-dot"></li>
    <li class="motion-dot"></li> <!-- Новий dot -->
</ul>
```

### 3. Оновіть JS (якщо потрібно):
В `motion-slider.js` слайдер автоматично визначає кількість слайдів, тому зміни не потрібні!

## ❌ Як видалити слайд

### 1. Видаліть HTML слайду
### 2. Видаліть відповідний dot:
```html
<ul class="motion-dots">
    <li class="motion-dot active"></li>
    <li class="motion-dot"></li>
    <li class="motion-dot"></li>
    <li class="motion-dot"></li>
    <!-- Видалено один dot -->
</ul>
```

## 🎨 Колірна схема

### Зміна кольору тексту:
В `motion-slider.css` знайдіть `.motion-slide-content h2`:
```css
.motion-slide-content h2 {
    color: #fff; /* Білий за замовчуванням */
    text-shadow: 0 2px 20px rgba(0, 0, 0, 0.5);
}
```

### Зміна кольору кнопок:
```css
.motion-progress-ring {
    background: rgba(255, 255, 255, 0.95); /* Біла кнопка */
}

.motion-progress-ring .progress-bar {
    stroke: #333; /* Чорний прогрес */
}
```

### Зміна кольору dots:
```css
.motion-dot {
    border: 2px solid rgba(255, 255, 255, 0.8);
    background: transparent;
}

.motion-dot.active {
    background: rgba(255, 255, 255, 0.9);
}
```

## ⚙️ Налаштування поведінки

### Швидкість автопрогресу:
В `motion-slider.js` знайдіть константи:
```javascript
const AUTO_DURATION = 11000; // 11 секунд автопрогрес (без курсора)
const PROGRESS_DURATION = 5500; // 5.5 секунд прогрес-кільце (з курсором)
```

Змініть значення (в мілісекундах):
- `3000` = 3 секунди
- `8000` = 8 секунд
- `11000` = 11 секунд (за замовчуванням для авто)
- `5500` = 5.5 секунд (за замовчуванням для кільця)

### Висота слайдера:
В `motion-slider.css`:
```css
.motion-slider {
    height: 80vh; /* 80% висоти екрану */
    min-height: 500px; /* Мінімальна висота */
}
```

## 📱 Мобільна версія

На мобілках автоматично:
- Показуються кнопки зліва/справа
- Працює swipe (проведення пальцем)
- Зменшується висота слайдера
- Текст адаптується

Налаштування в `motion-slider.css` breakpoints:
- Desktop: `> 1024px`
- Tablet: `768px - 1024px`
- Mobile: `<= 768px`
- Small mobile: `<= 480px`

## 🔍 Troubleshooting

### Слайдер не працює:
1. Перевірте, чи підключено `motion-slider.js`
2. Перевірте console на помилки (F12)
3. Перевірте, чи є клас `active` на першому слайді та першому dot

### Відео не відтворюється:
1. Переконайтесь, що додано атрибути `autoplay muted playsinline`
2. Перевірте шлях до відео
3. Перевірте формат (підтримується MP4, WebM)

### Прогрес-кільце не анімується:
1. Перевірте, чи наводиться курсор на слайдер
2. Перевірте console на JS помилки

## 📞 Підтримка

Якщо виникли проблеми:
1. Перевірте console браузера (F12)
2. Перевірте, чи всі файли підключені правильно
3. Перевірте структуру HTML (dots = слайди)

---

**Створено:** 2025-11-13  
**Версія:** 1.0.0  
**Автор:** CoreSync Development Team

