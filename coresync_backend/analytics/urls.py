"""
Analytics URLs for the CoreSync API.
"""
from django.urls import path

app_name = 'analytics'

urlpatterns = [
    # TODO: Add analytics endpoints
    # path('dashboard/', views.DashboardView.as_view(), name='dashboard'),
    # path('revenue/', views.RevenueAnalyticsView.as_view(), name='revenue'),
    # path('usage/', views.UsageAnalyticsView.as_view(), name='usage'),
]
