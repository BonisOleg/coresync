"""
Forms URLs for the CoreSync API.
"""
from django.urls import path

app_name = 'forms'

urlpatterns = [
    # TODO: Add form submission endpoints
    # path('contact/', views.ContactSubmissionView.as_view(), name='contact'),
    # path('membership/', views.MembershipInquiryView.as_view(), name='membership'),
    # path('newsletter/', views.NewsletterSubscriptionView.as_view(), name='newsletter'),
]
